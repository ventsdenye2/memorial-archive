# 场景重建记录（2026-09-10）

已更新 15 个游戏场景，包括新增 `Room_Terrace`。前厅和露台使用提供的压缩包素材，其余优先使用 8.4 版本；缺少对应新版的房间沿用现有素材。接待室对应客休室。按确认范围，不创建旧档案室、禁闭室，办公室解谜物品暂不制作。

## 尺寸与相机

所有高度为 1080 像素，PPU 为 100，相机视口为 1920×1080（正交尺寸 5.4）。**不把 1920 原图横向拉成 3840**。

| 场景 | 背景宽度 |
| --- | --- |
| FrontHall | 11520 |
| Office、Reception、ArchiveA、Director、TreatmentA/B、Terrace | 3840 |
| ArchiveB、ArchiveC、Toilet | 1920 |
| Floor_1F、Floor_2F、Floor_3F | 4 段原生 1920，共 7680 |
| Floor_4F | 2 段原生 1920，共 3840 |

碰撞边界、相机限制、门和楼梯落点、资源点随实际宽度调整。B/C 使用旧整图，补充可见门；可拆分的新版房间使用干净图层与独立电话/便签，避免整图与道具重复。旧场景物件保留为 inactive，当前布局集中在 `SceneLayout0909`。

## 资源与规则

- 8 个电话存档点、14 个便签点、118 个灯光节点、2 个怪物出生点；总活动交互组件为 183 个（包括普通灯与出入口）。
- 特殊灯统一复用指定的 `UpdatedCorridors/2F/Source/图层 8 副本 3.png`。房间各自控制普通灯；前厅与一楼走廊共用区域，露台与三楼走廊共用区域。
- 新增四楼钥匙 1027、馆长办公室钥匙 1028，暂复用已有钥匙图标。治疗室 A 配置办公室钥匙 1024；档案室 C 配置刺刀 1002 与四楼钥匙；治疗室 B 配置护腕 1005 与馆长钥匙；馆长办公室配置斧头 1003；露台配置绳索 1025。
- 门禁位于场景切换服务：二楼→办公室需要 1024；三楼→馆长办公室需要 1028；三楼→四楼需要 1027。返回路线自由通行，存档恢复不经过入口门禁。
- 档案室 C 生成普通怪物；二楼走廊生成强化怪物 2003，当前配置生命 2000、攻击 2，可在配置资产调整。
- 随机容器使用现有物品池；容器具有一次性初始化状态，取空后重进不会补货，旧存档中已有的空容器也不会重新填充。办公室容器有交互但内容为空。

## 已执行验证

使用本机 8080 Unity MCP，Unity 2022.3.62f2c1：

1. Unity 编译完成，检查期间 Console error 为 0。
2. Unity EditMode：InventorySystemTests + SceneAccessRuleTests **16/16 通过**。覆盖空容器重进、旧存档空容器、非法配置原子回滚，以及门禁方向。
3. 15 场景编辑器检查通过：资源/脚本引用、交互 ID、灯光注册、边界、出生点、原生背景缩放、怪物预制体引用。报告：`Temp/SceneRebuildAudit/unity_scene_audit.json`。
4. 15 场景均通过 Unity Camera.Render 输出检查。图片：`Temp/SceneRebuildAudit/Renders`。
5. Play Mode 中通过业务事件验证：前厅 1920×1080、相机 5.4；无办公室钥匙被拒绝、有钥匙成功进入；办公室空容器已初始化；档案室 C 容器为 `[1002,1027]` 且怪物实例生成；露台容器为 `[1025]`，返回馆长办公室落点正确；选中手提灯后，馆长特殊灯能点亮所属区域普通灯。运行时检查未写入玩家存档。
6. 28 条门/楼梯传送记录逐条检查目标场景与落点引用，通过。15 场景实体碰撞检查未发现遗留内部碰撞墙；183 个交互点经 Unity Collider2D.Distance 验证，与行走高度 -5.2 的玩家碰撞体均能重叠。对应报告为 `routes.json`、`colliders.json`、`reachability.json`，位于同一临时验证目录。

Play Mode 验证通过事件调用和状态读取执行，尚未逐个完成键鼠走图、所有存档槽写入/读取与战斗手感验收。便签沿用现有解锁事件与 ID，正文/日记展示和完整躲藏玩法没有在本次新增；不是完整剧情通关验收。

## 后续编辑

布局数据为 `Tools/SceneRebuild/layout.json`，复用脚本与操作步骤见同目录 README。Unity 菜单 **Tools → Memorial Archive → Validate Scene Layout** 可重复运行静态场景检查。重新生成前需退出 Play Mode、保存并关闭受影响场景，将手工调整同步回布局数据。

本次未提交 Git。工作区另有已有灯光系统/手提灯调整，未将其当作本次场景重建的实现或验证结论。
