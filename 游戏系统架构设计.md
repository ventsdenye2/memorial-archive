# Memorial Archive 游戏系统架构设计

> 本文用于后续讨论和修改游戏框架。目标不是做一个复杂炫技的框架，而是为一个小型 Unity 独立游戏提供清晰、可理解、低耦合、易落地的系统设计。

## 1. 设计目标

### 1.1 项目定位

本项目是一个小型 Unity 独立游戏，当前需求包含：

- 背包与道具系统
- 人物状态与动作系统
- 战斗系统
- 怪物逻辑系统
- 场景、房间与交互系统
- 解密系统
- 剧情、文案、日记与纸条系统
- UI 系统
- 存档管理系统
- ScriptableObject 配置系统

当前最新需求确认：

- 所有二级页面均为 UI Panel，不通过新建 Unity 场景实现。
- 剧情在独立 Unity 场景中播放；文案系统支持逐行推进、CG 切换、立绘聚焦和组合特效。
- 背包、场景物品栏、快捷栏、副手栏以拖拽为核心交互。
- 快捷栏数字键用于装备，不直接使用。
- 鼠标右键瞄准，`R` 换弹。
- 怪物刷新点和其他交互点建议做成预制体。

### 1.2 总体原则

1. **中度框架，不做过度工程化**
   - 使用 `GameRoot + GameContext + C# System + EventBus + ScriptableObject`。
   - 不引入 ECS、复杂依赖注入、代码热更、资源热更。
   - YooAsset、HybridCLR、复杂对象生命周期管理暂不进入第一版。

2. **逻辑层和实现层分开**
   - 规则、数据、计算、状态流转尽量使用纯 C# 类。
   - 输入、碰撞、动画、音效、UI、场景对象绑定使用 `MonoBehaviour`。

3. **系统之间低耦合**
   - 同系统内部可以直接调用。
   - 跨系统优先通过事件通信。
   - 少数确实需要稳定查询能力的地方使用接口。

4. **易理解优先**
   - 类名、目录名尽量直白。
   - 不使用过度抽象的命名。
   - 每个系统都写清楚“负责什么”和“不负责什么”。

5. **性能不过度牺牲**
   - 避免大量分散的 `Update`。
   - 第一版暂不引入对象池，优先控制运行时生成和销毁频率。
   - 配置数据启动时转成字典，运行时按 Id 查询。
   - UI、背包、剧情等尽量事件驱动，不每帧刷新。

## 2. 基础概念说明

### 2.1 GameRoot

`GameRoot` 是游戏启动入口，继承 `MonoBehaviour`。

它负责：

- 创建基础服务。
- 创建各个纯 C# 系统。
- 控制初始化顺序。
- 持有 `GameContext`。
- 统一调用需要更新的系统。
- 游戏退出时释放系统。

它不负责：

- 不写背包规则。
- 不写战斗计算。
- 不写怪物 AI。
- 不直接控制 UI 页面细节。
- 不直接开门、扣血、加道具。

### 2.2 GameContext

`GameContext` 是系统初始化时拿到的公共工具包。

它可以包含：

- `EventBus`
- `ConfigManager`
- `SaveManager`
- `SceneFlowManager`
- `UIManager`
- `GameLoop`

它不应该包含：

- `InventorySystem`
- `CharacterSystem`
- `CombatSystem`
- `MonsterSystem`

这样做是为了避免任何系统都能随便拿到其他系统，导致耦合变高。

### 2.3 System

本文中的 `System` 指纯 C# 玩法系统，不继承 `MonoBehaviour`。

例如：

- `InventorySystem`
- `CharacterSystem`
- `CombatSystem`
- `MonsterSystem`
- `InteractionSystem`
- `PuzzleSystem`
- `StorySystem`
- `DialogueSystem`

System 负责：

- 管理本系统数据。
- 执行业务规则。
- 响应事件。
- 发出事件。
- 提供存档数据。

### 2.4 View

`View` 是 Unity 场景里的表现层，一般继承 `MonoBehaviour`。

例如：

- `PlayerMotor`
- `PlayerAnimatorView`
- `MonsterView`
- `DoorView`
- `InventoryPanel`
- `ItemSlotView`
- `InteractionPointView`

View 负责：

- 读取或绑定 Unity 组件。
- 播放动画。
- 移动 `Transform` 或 `Rigidbody2D`。
- 响应碰撞和触发器。
- 刷新 UI。
- 播放音效。

View 不负责：

- 不写核心规则。
- 不保存关键玩法数据。
- 不做复杂战斗计算。

### 2.5 Tick

`Tick` 是纯 C# 系统的更新函数。

Unity 的 `Update()` 是 `MonoBehaviour` 每帧调用的方法；纯 C# 类没有 `Update()`，所以由 `GameRoot` 主动调用它们的 `Tick(deltaTime)`。

例子：

```csharp
public interface ITickableSystem
{
    void Tick(float deltaTime);
}
```

每调用一次 `Tick`，系统就根据过去的时间更新一次状态。

适合 Tick 的系统：

- 角色体力恢复或消耗。
- 状态持续时间。
- 怪物 AI 思考。
- Buff / Debuff 计时。

不适合 Tick 的系统：

- 背包。
- 剧情。
- 存档。
- 普通 UI 页面。

### 2.6 SaveModule

需要存档的系统需要告诉 `SaveManager`：

- 我要存什么。
- 读档时怎么恢复。

建议接口：

```csharp
public interface ISaveModule
{
    string ModuleKey { get; }
    object CaptureSaveData();
    void RestoreSaveData(object data);
}
```

存档时：

```text
SaveManager
-> 问 InventorySystem 要背包数据
-> 问 CharacterSystem 要角色数据
-> 问 StorySystem 要剧情数据
-> 问 PuzzleSystem 要谜题数据
-> 问 RoomSystem 要场景容器和交互状态
-> 问 MonsterSystem 要怪物和刷新点状态
-> 合成完整 SaveData
-> 写入硬盘
```

各系统不直接写硬盘，统一由 `SaveManager` 写入。

## 3. 推荐目录结构

建议 Unity 项目中使用以下结构：

```text
Memorial Archive/
└── Assets/
    ├── Scripts/
    │   ├── Framework/
    │   │   ├── Core/
    │   │   ├── Event/
    │   │   ├── Config/
    │   │   ├── Save/
    │   │   ├── UI/
    │   │   └── Scene/
    │   │
    │   └── Gameplay/
    │       ├── Character/
    │       │   ├── Logic/
    │       │   ├── View/
    │       │   ├── Data/
    │       │   └── Config/
    │       ├── Inventory/
    │       │   ├── Logic/
    │       │   ├── View/
    │       │   ├── Data/
    │       │   └── Config/
    │       ├── Item/
    │       │   ├── Logic/
    │       │   ├── Data/
    │       │   └── Config/
    │       ├── Combat/
    │       │   ├── Logic/
    │       │   ├── View/
    │       │   ├── Data/
    │       │   └── Config/
    │       ├── Monster/
    │       │   ├── Logic/
    │       │   ├── View/
    │       │   ├── Data/
    │       │   └── Config/
    │       ├── Interaction/
    │       │   ├── Logic/
    │       │   ├── View/
    │       │   ├── Data/
    │       │   └── Config/
    │       ├── Puzzle/
    │       │   ├── Logic/
    │       │   ├── Data/
    │       │   └── Config/
    │       ├── Story/
    │       │   ├── Logic/
    │       │   ├── View/
    │       │   ├── Data/
    │       │   └── Config/
    │       └── Camera/
    │       ├── Dialogue/
    │       │   ├── Logic/
    │       │   ├── View/
    │       │   ├── Data/
    │       │   └── Config/
    │           └── View/
    │
    ├── GameConfigs/
    │   ├── Items/
    │   ├── Character/
    │   ├── Monsters/
    │   ├── Interaction/
    │   ├── Puzzle/
    │   ├── Story/
    │   └── Dialogue/
    │
    ├── Prefabs/
    ├── Scenes/
    ├── UI/
    ├── Art/
    └── Audio/
```

目录含义：

- `Framework`：通用基础设施。
- `Gameplay`：具体游戏玩法。
- `Logic`：纯 C# 规则和流程。
- `View`：Unity 表现层。
- `Data`：运行时数据和存档数据结构。
- `Config`：ScriptableObject 配置定义。

## 4. 初始化流程

### 4.1 游戏启动流程

```text
GameRoot.Awake
-> 创建 EventBus
-> 创建 ConfigManager
-> 加载 ScriptableObject 配置
-> 创建 SaveManager
-> 创建 UIManager
-> 创建 SceneFlowManager
-> 创建 GameContext
-> 创建 Gameplay Systems
-> 初始化所有 System
-> 注册需要 Tick 的系统
-> 注册需要存档的系统
-> 进入主菜单或新游戏流程
```

### 4.2 系统初始化建议顺序

```text
1. EventBus
2. ConfigManager
3. SaveManager
4. UIManager
5. SceneFlowManager
6. CharacterSystem
7. InventorySystem
8. ItemEffectSystem
9. CombatSystem
10. InteractionSystem
11. PuzzleSystem
12. StorySystem
13. DialogueSystem
14. MonsterSystem
```

### 4.3 System 基础接口

```csharp
public interface IGameSystem
{
    void Initialize(GameContext context);
    void Dispose();
}
```

需要持续更新的系统：

```csharp
public interface ITickableSystem
{
    void Tick(float deltaTime);
}
```

需要存档的系统：

```csharp
public interface ISaveModule
{
    string ModuleKey { get; }
    object CaptureSaveData();
    void RestoreSaveData(object data);
}
```

## 5. 系统通信方式

### 5.1 直接调用

同一个系统内部可以直接调用。

例子：

```text
InventorySystem
-> InventoryGrid.TryPlaceItem()
-> InventoryData.AddItem()
```

### 5.2 事件通信

跨系统通信优先使用事件。

例子：

```text
InventorySystem 发 InventoryChangedEvent
InventoryPanel 收到后刷新 UI
```

```text
InteractionSystem 发 SaveRequestedEvent
SaveManager 收到后执行存档
```

### 5.3 接口查询

少数需要稳定查询能力的地方使用接口。

例子：

```text
CombatSystem 需要知道角色是否正在格挡
-> 查询 ICharacterCombatStateProvider
```

不要直接让 `CombatSystem` 拿完整的 `CharacterSystem`。

## 6. Framework 基础系统

### 6.1 EventBus

### 这个系统解决什么问题

`EventBus` 用于系统之间传递消息，避免系统互相直接引用。

### 它负责什么

- 注册事件监听。
- 取消事件监听。
- 派发事件。

### 它不负责什么

- 不保存玩法状态。
- 不决定业务规则。
- 不做异步队列，第一版同步派发即可。

### 第一版建议

使用类型作为事件键：

```csharp
eventBus.Subscribe<InventoryChangedEvent>(OnInventoryChanged);
eventBus.Publish(new InventoryChangedEvent());
```

### 注意事项

- `View` 销毁时必须取消监听。
- 不要用事件替代所有直接调用，同系统内部不需要事件。

### 6.2 ConfigManager

### 这个系统解决什么问题

统一管理 ScriptableObject 配置，运行时按 Id 查询。

### 它负责什么

- 启动时加载配置数据库。
- 将配置转成字典。
- 提供查询方法。

### 它不负责什么

- 不修改运行时状态。
- 不保存玩家存档。
- 不处理 UI 显示。

### 配置类型

- `ItemConfig`
- `WeaponConfig`
- `ConsumableConfig`
- `CharacterAttributeConfig`
- `CharacterStateConfig`
- `MonsterConfig`
- `InteractionConfig`
- `PuzzleConfig`
- `StoryEntryConfig`
- `DialogueSequenceConfig`
- `DialogueEffectConfig`

### 查询例子

```csharp
ItemConfig item = context.Configs.GetItem(itemId);
MonsterConfig monster = context.Configs.GetMonster(monsterId);
```

### 6.3 SaveManager

### 这个系统解决什么问题

统一保存和读取游戏进度。

### 它负责什么

- 管理存档槽。
- 收集各系统存档数据。
- 将完整存档写入硬盘。
- 从硬盘读取存档。
- 将存档数据分发给各系统恢复。

### 它不负责什么

- 不决定背包里有什么。
- 不决定剧情是否解锁。
- 不直接操作场景对象表现。

### 存档数据建议

```text
SaveData
- saveVersion
- saveTime
- currentSceneId
- currentRoomId
- characterData
- inventoryData
- storyData
- puzzleData
- roomData
- sceneContainerData
- monsterData
- monsterSpawnData
```

### 性能注意

- 手动存档时写硬盘。
- 不要每帧自动写硬盘。
- 自动存档如有需要，放在场景切换或关键节点。

### 6.4 UIManager

### 这个系统解决什么问题

统一管理 UI 页面打开、关闭和层级。

### 它负责什么

- 打开页面。
- 关闭页面。
- 维护当前页面栈。
- 暂停时打开系统页。
- 根据事件打开背包、日记、地图、系统、设置、存档、读档等 Panel。
- 控制文案 Panel 的显示、关闭和玩法输入锁定。

### 它不负责什么

- 不写背包格子规则。
- 不写道具效果。
- 不写存档数据。

### 第一版页面

- 游戏 HUD
- 主菜单
- 背包页
- 场景 2x2 物品栏
- 快捷栏和副手栏 UI
- 日记页
- 地图页
- 系统页
- 设置页
- 存档页
- 读档页
- 开始新游戏确认弹窗
- 文案页
- 交互提示

### 6.5 SceneFlowManager

### 这个系统解决什么问题

统一处理房间、楼梯、场景切换流程。

### 它负责什么

- 接收切换请求。
- 加载目标场景。
- 设置玩家出生点。
- 通知场景进入和离开。

### 它不负责什么

- 不判断玩家能否开门。
- 不判断谜题是否完成。
- 不管理背包。

### 第一版事件

- `SceneTransitionRequestedEvent`
- `SceneLoadedEvent`
- `RoomEnteredEvent`
- `RoomExitedEvent`

## 7. 输入与交互系统

### 7.1 输入系统

### 这个系统解决什么问题

读取键盘和鼠标输入，并转换成游戏可以理解的输入事件。

### 它由哪些部分组成

- `InputReader`：继承 `MonoBehaviour`，读取 Unity 输入。
- 输入事件：纯数据事件。

### InputReader 负责什么

- 读取移动输入。
- 读取奔跑、格挡、闪避。
- 读取交互键。
- 读取快捷栏数字键。
- 读取鼠标左键。
- 读取空格键，用于剧情文案推进。
- 读取鼠标右键瞄准。
- 读取 `R` 换弹。
- 读取背包、日记、地图、系统菜单等按键。

### InputReader 不负责什么

- 不移动角色。
- 不开门。
- 不加道具。
- 不扣血。

### 输入事件

- `MoveInputEvent`
- `RunInputEvent`
- `BlockInputEvent`
- `DialogueAdvancePressedEvent`（鼠标左键或空格，在文案输入模式下统一为一次事件）
- `DialogueInputModeChangedEvent`
- `DodgePressedEvent`
- `InteractPressedEvent`
- `ShortcutEquipPressedEvent`
- `PrimaryActionPressedEvent`
- `AimInputEvent`
- `ReloadPressedEvent`
- `OpenInventoryPressedEvent`
- `OpenDiaryPressedEvent`
- `OpenMapPressedEvent`
- `PausePressedEvent`

### 性能注意

输入读取可以在 `Update` 中进行，但只做轻量读取和事件发送。

当 `DialogueSystem` 处于播放状态时，`InputReader` 进入文案输入模式：鼠标左键与空格只发布 `DialogueAdvancePressedEvent`，不再同时转发为攻击等玩法输入。输入模式由 `UIManager` 的输入锁定或 `DialogueInputModeChangedEvent` 控制。

### 7.2 交互系统

### 这个系统解决什么问题

管理玩家当前附近有哪些可交互点，以及按下交互键后触发什么请求。

### 它由哪些部分组成

- `InteractionSystem`：纯 C#，管理当前可交互点。
- `InteractionPointView`：继承 `MonoBehaviour`，挂在场景交互点上。
- `InteractionData`：交互点运行时数据。
- `InteractionConfig`：交互点配置。

### InteractionSystem 负责什么

- 记录当前进入范围的交互点。
- 选择当前优先交互点。
- 收到交互输入后发出对应事件。
- 通知 UI 显示或隐藏交互提示。

### InteractionSystem 不负责什么

- 不直接打开 UI。
- 不直接执行存档。
- 不直接开门。
- 不直接切场景。

### InteractionPointView 负责什么

- 玩家进入触发范围时注册交互点。
- 玩家离开触发范围时取消交互点。
- 保存交互点 Id、类型、目标参数。

### 交互类型

```csharp
public enum InteractionType
{
    SavePoint,
    SceneEntranceLeftRight,
    SceneEntranceUp,
    Inspect,
    Stairs,
    PickupContainer,
    Door,
    Puzzle
}
```

### 交互事件

- `InteractionFocusChangedEvent`
- `SaveRequestedEvent`
- `OpenContainerRequestedEvent`
- `InspectRequestedEvent`
- `DoorInteractRequestedEvent`
- `PuzzleInteractRequestedEvent`
- `SceneTransitionRequestedEvent`

### 性能注意

- 不让每个交互点每帧算距离。
- 使用 Unity Trigger 注册和取消。
- 当前交互点变化时才刷新提示 UI。

## 8. 角色状态系统

### 这个系统解决什么问题

管理玩家生命、体力、速度、行为状态和装备修正。

### 它由哪些部分组成

- `CharacterSystem`：纯 C#，角色状态规则。
- `CharacterData`：纯数据，当前生命、体力、速度、装备、状态。
- `CharacterStateMachine`：纯 C#，判断状态切换是否合法。
- `PlayerMotor`：继承 `MonoBehaviour`，移动玩家对象。
- `PlayerAnimatorView`：继承 `MonoBehaviour`，播放动画。
- `PlayerAudioView`：继承 `MonoBehaviour`，播放音效。

### CharacterSystem 负责什么

- 初始化生命、体力、速度。
- 响应移动、奔跑、格挡、闪避、攻击输入。
- 判断当前能否行动。
- 扣除或恢复体力。
- 处理受伤、治疗、死亡。
- 计算当前移动速度。
- 发出角色状态变化事件。
- 提供存档数据。

### CharacterSystem 不负责什么

- 不直接移动 `Transform`。
- 不直接播放动画。
- 不直接读取键盘。
- 不直接刷新 UI 血条。

### 主状态

主状态表示玩家正在做什么：

- `Idle`
- `Walk`
- `Run`
- `Block`
- `ShieldBlock`
- `Dodge`
- `Attack`
- `Aim`
- `ThrowAim`
- `Hurt`
- `Dead`

### 状态修正

状态修正表示玩家身上的额外影响：

- `HeavyInjured`：生命值小于等于 1。
- `Armored`：装备钢夹板。
- `Buff`：药品或食品带来的临时强化。
- `Debuff`：流血、中毒等负面状态，第一版可后置。

这样做是为了避免出现大量组合状态，例如“重伤奔跑”“披甲重伤奔跑”。

### 体力规则

来自当前配置表：

- 体力初始值 30。
- 体力最大值 30。
- 奔跑每秒消耗 2。
- 格挡每秒消耗 2。
- 近战攻击消耗 6。
- 后撤闪避消耗 6。
- 格挡成功消耗 5。
- 站立和行走恢复体力。

建议规则：

- 体力不足时不能触发消耗型动作。
- 奔跑中体力为 0，自动切回行走。
- 格挡中体力为 0，自动退出格挡。

### 生命与防御规则

来自当前配置表：

- 生命初始值 3。
- 生命最大值 3。
- 普通受击扣 1。
- 装备钢夹板时物理伤害减少 50%，不包含格挡情况。
- 钢夹板减少 20% 移动速度。
- 持盾格挡成功生命扣 0，盾牌耐久减 1，体力扣 5。
- 防火盾格挡三次后报废并自动移除。
- 火焰伤害不消耗防火盾三次抵挡次数。
- 生命归零后播放死亡动画，随后打开读档 Panel；取消读档则返回主菜单。

### 速度规则

来自当前配置表：

- 行走速度 100。
- 奔跑速度 300。
- 重伤速度乘以 0.8。
- 披甲速度乘以 0.8。
- 披甲负伤速度乘以 0.6。

### 事件

监听：

- `MoveInputEvent`
- `RunInputEvent`
- `BlockInputEvent`
- `DodgePressedEvent`
- `PrimaryActionPressedEvent`
- `DamageAppliedEvent`
- `ItemEffectAppliedEvent`

发出：

- `CharacterStatsChangedEvent`
- `CharacterStateChangedEvent`
- `CharacterMoveIntentEvent`
- `PlayerAttackRequestedEvent`
- `CharacterDiedEvent`

### 存档内容

- 当前生命值。
- 当前体力值。
- 当前主状态。
- 当前装备。
- 当前 Buff / Debuff。
- 当前场景和房间位置可放在场景存档中，也可由角色系统记录。

## 9. 背包与道具系统

### 9.1 背包系统

### 这个系统解决什么问题

管理玩家 `3 * 3` 背包、快捷栏、副手栏、物品占格、拖拽交换和场景 `2 * 2` 物品栏。

### 它由哪些部分组成

- `InventorySystem`：纯 C#，背包规则。
- `InventoryData`：纯数据，背包和快捷栏内容。
- `InventoryGrid`：纯 C#，格子占用计算。
- `InventoryItemInstance`：纯数据，物品实例。
- `SceneContainerData`：纯数据，场景交互点保留的物品。
- `InventoryPanel`：继承 `MonoBehaviour`，背包 UI。
- `ItemSlotView`：继承 `MonoBehaviour`，单个格子显示和拖拽。
- `ContainerPanel`：继承 `MonoBehaviour`，场景 2x2 物品栏 UI。
- `ShortcutBarPanel`：继承 `MonoBehaviour`，快捷栏 UI。
- `OffhandSlotView`：继承 `MonoBehaviour`，副手栏 UI。

### InventorySystem 负责什么

- 添加物品。
- 移除物品。
- 移动物品。
- 判断物品是否能放入。
- 支持 1 格和 2 格物品。
- 处理玩家背包和场景容器之间交换。
- 管理快捷栏绑定。
- 管理数字键装备的当前快捷栏物品。
- 管理单槽副手栏。
- 管理拖出 UI 后丢弃到场景容器。
- 判断背包满时拾取失败。
- 提供存档数据。

### InventorySystem 不负责什么

- 不直接刷新 UI。
- 不直接播放拖拽音效。
- 不直接执行回血、攻击、开门等效果。

### 背包格子规则

- 玩家背包大小为 `3 * 3`。
- 场景物品栏大小为 `2 * 2`。
- 道具占格根据 `ItemConfig.Placeholder`。
- 背包格子为长方形。
- 1 格物品占 1 个格子。
- 2 格物品是横向 `1 * 2`，只允许横放。
- 不做竖放逻辑。
- 不做旋转逻辑。

### 快捷栏规则

- 快捷栏通过拖拽绑定物品。
- 快捷栏和副手栏内所有物品都只占 1 格。
- 数字键 `1/2/3` 装备对应快捷栏物品。
- 只有配置中允许装备到快捷栏的物品才能绑定。
- 装备后鼠标左键根据物品类型执行攻击、射击、投掷、消耗品使用或弹药上膛/换弹。

### 副手栏规则

- 副手栏只有一个槽。
- 钢夹板、手提灯、盾互斥。
- 卸下副手装备需要打开背包并拖拽。
- 拖到背包或快捷栏 UI 内时保留。
- 拖到 UI 外时丢弃到当前场景物品栏。

### 场景容器规则

- 场景物品交互点打开后，UIManager 同时打开背包、场景 2x2 物品栏和下方快捷栏。
- 场景容器显示该交互点周围可放入背包的物品。
- 背包、快捷栏、副手栏中的物品会被角色带走。
- 场景容器中剩余物品保留在原交互点。
- 关闭容器后剩余物品不消失。

### 事件

监听：

- `OpenInventoryPressedEvent`
- `OpenContainerRequestedEvent`
- `ShortcutEquipPressedEvent`
- `PrimaryActionPressedEvent`
- `ItemPickupRequestedEvent`

发出：

- `InventoryChangedEvent`
- `ShortcutChangedEvent`
- `SelectedItemChangedEvent`
- `ItemUseRequestedEvent`
- `ContainerOpenedEvent`
- `ContainerClosedEvent`

### 存档内容

- 背包物品列表。
- 每个物品实例 Id。
- 每个物品配置 Id。
- 每个物品所在格子。
- 快捷栏绑定。
- 当前装备的快捷栏。
- 副手栏物品。
- 场景容器剩余物品由 RoomSystem 或独立场景容器存档保存。

### 9.2 道具配置系统

### 这个系统解决什么问题

用 ScriptableObject 表达道具配置，供背包、战斗、角色状态、剧情和解密查询。

### 配置建议

```text
ItemConfig
- itemId
- itemName
- description
- itemType
- isMissionItem
- isConsumable
- placeholder
- quickAccessType
- icon
- canStack
- stackMax
```

```text
WeaponConfig : ItemConfig
- damage
- attackDuration
- attackRange
- staminaCost
- weaponHandType
```

```text
ConsumableConfig : ItemConfig
- effectType
- effectValue
- duration
```

### 注意

第一版可以用一个 `ItemConfig` 包含所有字段，减少类数量。等配置变复杂后再拆 `WeaponConfig` 和 `ConsumableConfig`。

### 9.3 道具效果系统

### 这个系统解决什么问题

把“使用道具”与“背包管理”分开。

### 它由哪些部分组成

- `ItemEffectSystem`：纯 C#，处理道具使用效果。
- `ItemEffectData`：纯数据，描述一次效果。

### ItemEffectSystem 负责什么

- 收到 `ItemUseRequestedEvent`。
- 查询道具配置。
- 判断道具能否使用。
- 将效果转成事件。
- 处理弹药使用、上膛和换弹请求。

### 它不负责什么

- 不管理背包格子。
- 不直接刷新 UI。
- 不直接移动角色。

### 效果类型

第一版建议：

- 恢复生命。
- 恢复体力。
- 一段时间不消耗体力。
- 一段时间近战伤害提升。
- 使用后恢复全部生命，结束后生命调整回使用前，体力归零。
- 开启门锁或场景交互。
- 弹药上膛或换弹。

### 事件

监听：

- `ItemUseRequestedEvent`
- `ReloadPressedEvent`

发出：

- `ItemEffectAppliedEvent`
- `ConsumableUsedEvent`
- `AmmoReloadRequestedEvent`
- `ItemUseFailedEvent`

## 10. 战斗系统

### 这个系统解决什么问题

处理攻击是否合法、伤害如何计算、格挡如何生效、体力如何消耗、攻击结果是什么。

### 它由哪些部分组成

- `CombatSystem`：纯 C#，战斗结算。
- `DamageRequest`：纯数据，一次伤害请求。
- `DamageResult`：纯数据，一次伤害结果。
- `HitboxView`：继承 `MonoBehaviour`，攻击判定。
- `ProjectileView`：继承 `MonoBehaviour`，投掷物或子弹表现。

### CombatSystem 负责什么

- 判断攻击者是否能攻击。
- 根据武器配置读取伤害、范围、体力消耗。
- 处理近战伤害。
- 处理枪械瞄准、射击、弹药消耗和换弹。
- 处理投掷物爆炸伤害。
- 处理格挡、持盾格挡、钢夹板减伤。
- 输出最终伤害结果。

### CombatSystem 不负责什么

- 不播放攻击动画。
- 不移动投掷物。
- 不检测 Unity 碰撞。
- 不直接刷新血条。

### 伤害流程

```text
攻击请求
-> 检查攻击者状态和体力
-> 读取武器配置
-> 创建 DamageRequest
-> 命中检测由 View 层完成
-> CombatSystem 计算 DamageResult
-> 发送 DamageAppliedEvent
-> 角色或怪物系统更新生命
```

### 第一版攻击类型

- 近战攻击。
- 手枪射击。
- 霰弹枪射击。
- 手雷投掷。
- 燃烧瓶投掷可后置。

### 事件

监听：

- `PlayerAttackRequestedEvent`
- `AimInputEvent`
- `ReloadPressedEvent`
- `AmmoReloadRequestedEvent`
- `HitboxHitEvent`
- `ProjectileExplodedEvent`

发出：

- `AttackStartedEvent`
- `ReloadStartedEvent`
- `ReloadCompletedEvent`
- `DamageAppliedEvent`
- `AttackFailedEvent`

### 性能注意

- 第一版不使用对象池；投掷物、命中特效等先按实际频率直接创建和销毁。
- 如果后续 Profiler 显示生成/销毁造成卡顿，再单独补小型对象复用方案。
- 怪物和玩家命中检测交给 Unity Collider，不用每帧手写全场扫描。
- 战斗计算只在攻击和命中时发生。

## 11. 怪物系统

### 这个系统解决什么问题

管理怪物运行时状态、AI 决策、追击、攻击、受击和死亡。

### 它由哪些部分组成

- `MonsterSystem`：纯 C#，管理怪物逻辑。
- `MonsterData`：纯数据，怪物当前生命、状态、目标。
- `MonsterBrain`：纯 C#，单个怪物 AI 决策。
- `MonsterStateMachine`：纯 C#，怪物状态切换。
- `MonsterView`：继承 `MonoBehaviour`，怪物场景表现。
- `MonsterAnimatorView`：继承 `MonoBehaviour`，怪物动画。
- `MonsterSpawnPointView`：继承 `MonoBehaviour`，场景中的怪物刷新点预制体。

### MonsterSystem 负责什么

- 注册当前场景怪物。
- 注销离开场景或死亡怪物。
- 在玩家进入场景后读取场景内所有怪物刷新点并刷新怪物。
- 按固定间隔 Tick 怪物 AI。
- 判断追击、攻击、受击、死亡状态。
- 管理怪物跟随玩家进房间的逻辑。
- 提供存档数据。

### MonsterSystem 不负责什么

- 不直接移动 `Transform`。
- 不直接播放动画。
- 不直接实例化 UI。

### 怪物类型

当前需求中有：

- 怪物 1：近战。
- 怪物 2：远程。

### 怪物状态

第一版建议：

- `Idle`
- `Patrol`，如果暂无巡逻可不做。
- `Chase`
- `Attack`
- `Hurt`
- `Stagger`
- `Dead`

### AI Tick

怪物 AI 不需要每帧更新。

建议：

- `0.1s` 或 `0.2s` 执行一次 AI Tick。
- 移动表现可以由 `MonsterView` 每帧平滑移动。

### 事件

监听：

- `RoomEnteredEvent`
- `DamageAppliedEvent`
- `CharacterDiedEvent`

发出：

- `MonsterStateChangedEvent`
- `MonsterMoveIntentEvent`
- `MonsterAttackRequestedEvent`
- `MonsterDiedEvent`

### 存档内容

- 怪物实例 Id。
- 怪物配置 Id。
- 当前生命。
- 当前状态。
- 当前房间。
- 是否死亡。
- 怪物刷新点 Id。
- 刷新点是否已触发。

### 怪物刷新点

- 怪物刷新点做成预制体。
- 玩家进入对应场景后，场景中所有怪物刷新点刷新怪物。
- 刷新点应配置怪物类型、位置、是否重复刷新等数据。
- 复杂刷新规则后续再扩展。

### 待完善

原需求中明确有待完善内容：

- 部分任务房间怪物不跟随。
- 躲藏系统。
- 复杂怪物刷新方式。

这些先留扩展点，不在第一版写死。

## 12. 场景、房间与摄像机系统

### 12.1 场景与房间系统

### 这个系统解决什么问题

管理房间、走廊、楼梯、场景切换和玩家出生点。

### 它由哪些部分组成

- `SceneFlowManager`：框架层，处理加载流程。
- `RoomSystem`：纯 C#，记录当前房间状态。
- `RoomController`：继承 `MonoBehaviour`，当前场景中的房间控制器。
- `DoorView`：继承 `MonoBehaviour`，门或入口表现。
- `SpawnPointView`：继承 `MonoBehaviour`，出生点。
- `InteractionPointView`：各类交互点预制体的通用表现层。

### RoomSystem 负责什么

- 记录当前房间 Id。
- 记录已经访问过的房间。
- 记录房间交互状态。
- 记录场景物品容器中遗留的物品。
- 记录怪物刷新点状态。
- 提供房间相关存档数据。

### RoomSystem 不负责什么

- 不加载 Unity 场景。
- 不播放开门动画。
- 不判断钥匙是否足够。

### 当前场景规则

来自需求：

- 当前单场景大小为 `1920 * 1080`。
- 后续房间可能扩展为 `3840 * 1080`。
- 楼梯长度为 `640`。
- 展览厅长度为 `5760`。
- 固定镜头需要改为镜头跟随角色。
- 走廊经过房间或楼梯时，应弹出交互按钮，不能直接跳转。
- 一楼大厅不用走廊。
- 障碍物代表玩家不能通过。
- 各类交互点建议制作成预制体，包括保存点、入口、楼梯、查看点、物品点、门、谜题点、怪物刷新点。

### 事件

监听：

- `SceneTransitionRequestedEvent`
- `DoorUnlockedEvent`

发出：

- `RoomEnteredEvent`
- `RoomExitedEvent`
- `SceneLoadedEvent`

### 12.2 摄像机系统

### 这个系统解决什么问题

让镜头跟随角色移动，并限制在当前房间边界内。

### 它由哪些部分组成

- `CameraFollowView`：继承 `MonoBehaviour`。
- `CameraBoundsView`：继承 `MonoBehaviour`，房间镜头边界。

### 它负责什么

- 跟随玩家。
- 根据房间范围限制镜头。
- 场景切换时读取新边界。

### 它不负责什么

- 不管理房间切换。
- 不处理玩家输入。

## 13. 解密系统

### 这个系统解决什么问题

管理谜题条件、任务道具使用、门锁、机关和场景交互结果。

### 它由哪些部分组成

- `PuzzleSystem`：纯 C#，判断谜题条件。
- `PuzzleData`：纯数据，谜题状态。
- `PuzzleConfig`：ScriptableObject，谜题配置。
- `PuzzleView`：继承 `MonoBehaviour`，场景机关表现。

### PuzzleSystem 负责什么

- 判断玩家是否拥有所需任务道具。
- 判断谜题是否已经完成。
- 处理门锁、机关、密码、纸条提示等逻辑。
- 发出成功或失败事件。
- 提供存档数据。

### PuzzleSystem 不负责什么

- 不管理背包格子。
- 不直接播放开门动画。
- 不直接显示 UI。

### 当前已知解密相关道具

- `1023 纸条`：提供谜题提示或密码。
- `1024 办公室钥匙`：打开办公室门锁。
- `1025 绳索`：用于场景交互。
- `1026 霰弹枪零件`：集齐 4 个可合成霰弹枪。

### 事件

监听：

- `PuzzleInteractRequestedEvent`
- `DoorInteractRequestedEvent`
- `ItemEffectAppliedEvent`

发出：

- `PuzzleSolvedEvent`
- `PuzzleFailedEvent`
- `DoorUnlockedEvent`
- `PuzzleHintUnlockedEvent`

### 存档内容

- 已完成谜题 Id。
- 已解锁门 Id。
- 已触发机关 Id。
- 已查看提示 Id。

## 14. 剧情、文案、日记与纸条系统

### 这个系统解决什么问题

管理剧情解锁、独立剧情场景中的文案播放、日记和纸条收集。剧情场景负责承载表现，文案逻辑仍由纯 C# 系统统一控制。

### 它由哪些部分组成

- `StorySystem`：纯 C#，剧情解锁逻辑。
- `StoryData`：纯数据，已解锁内容。
- `StoryEntryConfig`：ScriptableObject，剧情条目配置。
- `DialogueSystem`：纯 C#，管理文案序列、当前节点和推进状态。
- `DialogueData`：纯数据，当前播放状态和节点快照。
- `DialogueSequenceConfig`：ScriptableObject，一段文案的节点列表。
- `DialogueEffectConfig`：ScriptableObject，一次点击触发的组合特效配置。
- `DialogueSceneController`：继承 `MonoBehaviour`，独立剧情场景的入口和结束跳转。
- `DialoguePanel`：继承 `MonoBehaviour`，剧情场景内的文案 UI Panel。
- `DialoguePresentationView`：继承 `MonoBehaviour`，显示文本、CG、立绘和特效。
- `DiaryPanel`：继承 `MonoBehaviour`，日记 UI。

### StorySystem 负责什么

- 收集日记。
- 收集纸条。
- 解锁剧情片段。
- 判断某条剧情是否已经解锁。
- 请求进入对应的独立剧情场景。
- 判断剧情场景结束后应解锁的内容。
- 提供 UI 查询数据。
- 提供存档数据。

### StorySystem 不负责什么

- 不直接刷新 UI。
- 不操作 `Image`、`AudioSource`、`Animator`、`SceneManager` 或协程。
- 不直接管理背包格子。

### 当前需求规则

- 日记收集后解锁对应剧情片段，了解档案馆历史与事故真相。
- 日记收集后跳转到日记页，由玩家手动关闭。
- 纸条收集后获得对应谜题提示或密码。
- 日记和纸条无法使用，无其他效果。
- 剧情开始时由 `SceneFlowManager` 进入独立剧情场景，场景内不生成可操作玩家。
- `DialogueSceneController` 在场景加载完成后发起 `DialoguePlayRequestedEvent`，结束后发出场景切换请求。
- 一次有效的鼠标左键单击或空格只推进一个文案节点；鼠标与空格同帧只算一次。
- 节点同时携带文本、可选 CG、说话角色和特效命令，切换时由一个节点事件原子更新全部表现。
- 当前说话角色立绘正常显示；其他可见角色叠加灰色半透明蒙版。旁白节点的聚焦规则由配置决定。
- CG 支持保持、替换和清除；未配置时保持上一张 CG。
- 闪电等特效配置为一个组合效果，由 View 在一次请求中完成雷声音效和屏幕快速闪烁；`blocksAdvance` 为真时效果完成前忽略推进输入。
- 最后一个节点再次收到有效推进输入后结束文案，发布 `DialogueFinishedEvent`，由场景控制器决定下一场景。

### 事件

监听：

- `StoryItemCollectedEvent`
- `ItemPickupRequestedEvent`

发出：

- `StoryUnlockedEvent`
- `NoteUnlockedEvent`
- `DiaryUpdatedEvent`
- `SceneTransitionRequestedEvent`（目标为剧情场景）

### 存档内容

- 已解锁日记 Id。
- 已解锁纸条 Id。
- 已完成剧情/文案 Id。
- 第一版不保存特效播放进度；若支持剧情中途存档，再保存 `dialogueId + nodeIndex`。

### 独立剧情场景约束

- 场景必须放入 Build Settings/场景注册表，并由 `SceneFlowManager` 统一加载。
- 场景内只放 `DialogueSceneController`、文案 Panel、CG/立绘挂点、闪屏层和音频表现组件。
- `DialogueSceneController` 不推进节点，不修改文案数据，只负责发起播放和监听结束事件。
- 离开剧情场景时销毁场景表现对象，`GameRoot`、`GameContext` 和纯 C# 系统继续持久存在。
### 文案事件

监听：

- `DialoguePlayRequestedEvent`
- `DialogueAdvancePressedEvent`
- `DialogueBlockingEffectFinishedEvent`

发出：

- `DialogueNodePresentedEvent`
- `DialogueFinishedEvent`
### 文案配置建议

`DialogueSequenceConfig`：

- `dialogueId`
- `nodes[]`

`DialogueNodeConfig`（序列化嵌套数据）：

- `nodeId`
- `speakerId`，旁白为空
- `text`
- `cgCommand`：`Keep`、`Set`、`Clear`
- `portraitStates[]`：角色 Id、立绘资源、位置槽位、是否可见
- `effectIds[]`
- `blocksAdvance`

`DialogueEffectConfig`：

- `effectId`
- `audioClipId`
- `screenFlashColor`
- `flashCount`
- `flashInDuration`、`flashOutDuration`
- `blocksAdvance`
### DialogueSystem 不负责什么

- 不读取 Unity 输入，不直接操作场景或 UI。
- 不决定立绘、CG、音效资源如何加载。
- 不在逻辑层实现闪屏、协程、动画和打字机表现。
- 不把雷声和闪屏拆成两个需要玩家分别点击的节点。
### DialogueSystem 负责什么

- 按 `dialogueId` 加载 `DialogueSequenceConfig`，校验节点索引。
- 维护 `Idle`、`Presenting`、`BlockingEffect`、`Finished` 等播放状态。
- 接收 `DialogueAdvancePressedEvent`，每次最多推进一个节点。
- 发布包含完整节点快照的 `DialogueNodePresentedEvent`。
- 在最后一个节点推进后发布 `DialogueFinishedEvent`。
- 提供当前文案 Id、节点索引和是否可推进的查询。
## 15. UI 系统

### 这个系统解决什么问题

管理主菜单、游戏内 HUD、背包、日记、地图、系统页、设置页、存档/读档页、确认弹窗、剧情场景文案页和交互提示。

### 它由哪些部分组成

- `UIManager`：管理页面打开和关闭。
- `BasePanel`：页面基类。
- `HUDPanel`
- `MainMenuPanel`
- `InventoryPanel`
- `ContainerPanel`
- `ShortcutBarPanel`
- `OffhandSlotView`
- `DiaryPanel`
- `MapPanel`
- `SystemPanel`
- `SettingsPanel`
- `SavePanel`
- `LoadPanel`
- `DialoguePanel`
- `DialoguePresentationView`
- `ConfirmDialogPanel`
- `InteractionPromptView`

### UIManager 负责什么

- 打开页面。
- 关闭页面。
- 页面层级。
- 页面栈。
- 输入锁定。
- 按 Panel 类型控制游戏是否暂停。

### UIManager 不负责什么

- 不写背包规则。
- 不写角色血量计算。
- 不写存档文件。

### UI 页面刷新原则

- 通过事件刷新。
- 不在 UI 中每帧查系统数据。
- 页面打开时主动拉一次数据。
- 数据变化后由事件通知刷新。

### HUD

显示：

- 生命值。
- 体力值。
- 快捷栏。
- 背包入口。
- 日记本入口。
- 地图入口。
- 系统入口。
- 任务或系统提示。

监听：

- `CharacterStatsChangedEvent`
- `ShortcutChangedEvent`
- `SelectedItemChangedEvent`

### 主菜单

显示：

- 新游戏。
- 继续游戏。
- 读取存档。
- 设置。
- 退出游戏。

### 设置页

显示：

- 全屏模式。
- 窗口模式。
- 分辨率选择。
- 背景音量。
- 音效音量。
- 保存设置。
- 取消。

### 背包页

显示：

- 3x3 背包。
- 2x2 场景物品栏，只有从物品交互点打开时显示。
- 下方快捷栏。
- 单槽副手栏。
- 物品介绍框。
- 选择和取消按钮。
- 选中态和置灰态。

监听：

- `InventoryChangedEvent`
- `ContainerOpenedEvent`

### 日记页

显示：

- 日记列表。
- 纸条列表。
- 翻页按钮。
- 关闭按钮。

监听：

- `DiaryUpdatedEvent`

### 系统页

显示：

- 暂停游戏。
- 继续。
- 设置。
- 读取存档。
- 返回主菜单。

### 读档页

显示：

- 存档槽列表。
- 取消。

用途：

- 主菜单读取存档。
- 游戏内系统页读取存档。
- 死亡动画结束后读取存档。

监听：

- `PausePressedEvent`

发出：

- `ResumeRequestedEvent`
- `LoadGameRequestedEvent`
- `ReturnMainMenuRequestedEvent`
- `OpenSettingsRequestedEvent`
- `OpenLoadPanelRequestedEvent`

## 16. 存档管理系统

### 这个系统解决什么问题

保存和读取玩家进度。

### 它由哪些部分组成

- `SaveManager`：统一存档入口。
- `SaveData`：完整存档数据。
- `ISaveModule`：需要存档的系统实现。
- `SaveSlotInfo`：存档槽概要信息。

### SaveManager 负责什么

- 新游戏。
- 保存游戏。
- 读取游戏。
- 删除或覆盖存档。
- 提供存档槽列表。

### SaveManager 不负责什么

- 不决定剧情解锁。
- 不决定背包规则。
- 不决定怪物 AI。

### 需要注册存档的系统

- `CharacterSystem`
- `InventorySystem`
- `StorySystem`
- `PuzzleSystem`
- `RoomSystem`
- `MonsterSystem`

### 存档触发方式

当前需求中有保存交互点。

第一版建议：

- 玩家在存档点按交互键。
- `InteractionSystem` 发 `SaveRequestedEvent`。
- `SaveManager` 收集系统数据。
- 写入硬盘。
- 发出 `SaveCompletedEvent`。

### 死亡读档流程

- `CharacterSystem` 发 `CharacterDiedEvent`。
- `PlayerAnimatorView` 播放死亡动画。
- 死亡动画结束后 `UIManager` 打开 `LoadPanel`。
- 玩家取消读档时返回主菜单。

### 文件格式

第一版建议使用 JSON。

原因：

- 易读。
- 易调试。
- 小项目数据量不大。

## 17. 配置系统

### 这个系统解决什么问题

用 ScriptableObject 管理策划可配置数据。

### 为什么选择 ScriptableObject

- 对 Unity 小项目友好。
- Inspector 可编辑。
- 不需要第一版就做 Excel 导入工具。
- 后续仍可从 Excel 转换生成。

### 配置目录

```text
Assets/GameConfigs/
├── Items/
├── Character/
├── Monsters/
├── Interaction/
├── Puzzle/
└── Story/
```

### 配置数据库

建议每类配置有一个 Database：

- `ItemDatabase`
- `CharacterDatabase`
- `MonsterDatabase`
- `InteractionDatabase`
- `PuzzleDatabase`
- `StoryDatabase`

每个 Database 里保存配置列表，启动时转成字典。

### 配置设计原则

- 每条配置必须有唯一 Id。
- Id 与当前配置表保持一致。
- 不在逻辑代码里硬编码大量数值。
- 运行时查询使用字典。

## 18. 系统之间的主流程

### 18.1 拾取物品流程

```text
玩家靠近物品交互点
-> InteractionPointView 注册交互点
-> UI 显示拾取提示
-> 玩家按交互键
-> InteractionSystem 发 OpenContainerRequestedEvent
-> InventorySystem 准备 2x2 容器数据
-> UIManager 打开背包页、容器页、快捷栏和副手栏
-> 玩家拖拽物品
-> InventorySystem 修改背包和容器数据
-> 发 InventoryChangedEvent
-> UI 刷新
-> 关闭容器时，背包/快捷栏/副手栏中的物品由玩家带走
-> 容器剩余物品保存回原交互点
```

### 18.2 快捷栏装备与使用流程

```text
玩家拖拽物品到快捷栏
-> InventorySystem 绑定快捷栏
-> 玩家按数字键 1/2/3
-> InventorySystem 装备对应快捷栏物品
-> 玩家点击鼠标左键
-> 根据装备物品类型进入攻击、射击、投掷、消耗品使用或弹药上膛/换弹流程
```

### 18.3 近战攻击流程

```text
玩家点击鼠标左键
-> CharacterSystem 判断能否攻击和体力是否足够
-> 发 PlayerAttackRequestedEvent
-> CombatSystem 读取武器配置
-> PlayerAnimatorView 播放攻击动画
-> HitboxView 在攻击窗口开启判定
-> 命中怪物后发 HitboxHitEvent
-> CombatSystem 计算 DamageResult
-> MonsterSystem 扣血并切状态
-> MonsterView 播放受击或死亡表现
```

### 18.4 枪械瞄准与换弹流程

```text
玩家装备枪械
-> 玩家按住或点击鼠标右键
-> InputReader 发 AimInputEvent
-> CharacterSystem 进入 Aim 状态
-> 鼠标位置与角色中心形成射线，射线不显示
-> 玩家点击鼠标左键
-> CombatSystem 判断弹药并射击
-> 玩家按 R 或使用快捷栏对应弹药
-> CombatSystem / ItemEffectSystem 执行上膛或换弹
```

### 18.5 开门流程

```text
玩家靠近门
-> InteractionSystem 记录门交互点
-> 玩家按交互键
-> InteractionSystem 发 DoorInteractRequestedEvent
-> PuzzleSystem 判断是否需要钥匙或谜题条件
-> 成功则发 DoorUnlockedEvent
-> DoorView 播放开门或请求场景切换
-> SceneFlowManager 切换场景或房间
```

### 18.6 存档流程

```text
玩家靠近存档点
-> 玩家按交互键
-> InteractionSystem 发 SaveRequestedEvent
-> SaveManager 请求各 ISaveModule 交出数据
-> SaveManager 合成 SaveData
-> SaveManager 写入硬盘
-> UI 显示保存成功
```

### 18.7 死亡流程

```text
玩家生命归零
-> CharacterSystem 发 CharacterDiedEvent
-> PlayerAnimatorView 播放死亡动画
-> UIManager 打开 LoadPanel
-> 玩家读档则 SaveManager 恢复存档
-> 玩家取消则返回主菜单
```

### 18.8 独立剧情场景与文案播放流程

```text
触发剧情条件
-> StorySystem 发 SceneTransitionRequestedEvent（目标为剧情场景）
-> SceneFlowManager 加载独立剧情场景
-> 场景加载完成，DialogueSceneController 发 DialoguePlayRequestedEvent
-> DialogueSystem 校验并呈现第一个节点
-> InputReader 在文案输入模式下读取鼠标左键或空格
-> DialogueSystem 收到 DialogueAdvancePressedEvent，每次推进一个节点
-> DialogueNodePresentedEvent 携带文本、CG、说话角色和特效命令
-> DialoguePresentationView 原子更新文本、CG、立绘蒙版并播放组合特效
-> blocksAdvance 为真时，特效完成前忽略推进输入
-> 最后一个节点再次推进，DialogueSystem 发 DialogueFinishedEvent
-> DialogueSceneController 请求 SceneFlowManager 进入下一场景
```

## 19. 性能与实现约束

### 19.1 Update 使用规则

允许使用 `Update` 的对象：

- `GameRoot`
- `InputReader`
- `PlayerMotor`
- `CameraFollowView`
- 少数需要平滑表现的 View

不建议每个系统都写自己的 `Update`。

### 19.2 Tick 使用规则

需要 Tick 的纯 C# 系统：

- `CharacterSystem`
- `MonsterSystem`
- Buff / Debuff 相关系统，第一版可放在 `CharacterSystem`

不需要 Tick 的系统：

- `InventorySystem`
- `StorySystem`
- `PuzzleSystem`
- `SaveManager`
- `DialogueSystem`
- `DialoguePresentationView` 仅在特效播放期间运行短协程，不参与逻辑 Tick。
- 大部分 UI

### 19.3 运行时对象创建

第一版暂不使用对象池。

实现约束：

- 不为了架构完整性提前实现对象池。
- 投掷物、飞行刀、命中特效、伤害数字等对象先按普通方式创建和销毁。
- 若后续实际运行中出现明显卡顿或 GC 抖动，再针对具体对象补小型复用方案。

### 19.4 配置查询

- 启动时把配置表转字典。
- 运行时不要用列表反复查找 Id。
- 不在战斗或怪物 Tick 中频繁分配临时对象。

### 19.5 UI 刷新

- 数据变化时刷新。
- 页面打开时刷新。
- 不要每帧重建背包格子。
- 文案节点变化时一次性刷新文本、CG 和立绘蒙版，不在 `Update` 中轮询。

## 20. 第一版开发优先级

### 20.1 第一阶段：可玩闭环

目标：玩家能从主菜单进入游戏，在房间中移动、交互、拾取、打开背包、拖拽物品、保存。

优先系统：

1. `GameRoot`
2. `EventBus`
3. `ConfigManager`
4. `InputReader`
5. `InteractionSystem`
6. `InventorySystem`
7. `UIManager`
8. `SaveManager`
9. `SceneContainerData`

### 20.2 第二阶段：角色与战斗

目标：玩家能攻击、瞄准、换弹、消耗体力、受伤、格挡，怪物能刷新和受击。

优先系统：

1. `CharacterSystem`
2. `CombatSystem`
3. `MonsterSystem`
4. `HitboxView`
5. `PlayerAnimatorView`
6. `MonsterView`
7. `MonsterSpawnPointView`

### 20.3 第三阶段：场景、解密与剧情

目标：房间切换、门锁、纸条提示、日记解锁和独立剧情场景中的文案播放。

优先系统：

1. `SceneFlowManager`
2. `RoomSystem`
3. `PuzzleSystem`
4. `StorySystem`
5. `DialogueSystem`
6. `DialoguePanel` / `DialoguePresentationView`
7. `DiaryPanel`
8. `MapPanel`

### 20.4 第四阶段：完善与优化

目标：补充怪物跟随、躲藏系统、更多道具效果、音效和表现。

可后置内容：

- 躲藏系统。
- 复杂怪物刷新规则。
- 燃烧、中毒、流血等复杂状态。
- 更复杂的地图显示。
- Excel 到 ScriptableObject 的导入工具。

## 21. 当前暂不做的内容

为了控制工程量，第一版暂不做：

- 资源热更新。
- 代码热更新。
- 网络系统。
- 复杂 ECS。
- 完整行为树。
- 大型资源管理框架。
- 自动化配置导入工具。
- 复杂编辑器工具链。

这些不是永远不做，而是等游戏闭环稳定后再评估。

## 22. 后续讨论顺序建议

后续围绕本文可以按以下顺序继续提问和修改：

1. 总目录和命名规范。
2. `GameRoot` 和初始化流程。
3. `EventBus` 事件设计。
4. ScriptableObject 配置结构。
5. 输入与交互系统。
6. 背包与道具系统。
7. 角色状态系统。
8. 战斗系统。
9. 怪物系统。
10. 场景与房间系统。
11. 解密系统。
12. 剧情与文案系统。
13. UI 系统。
14. 存档系统。

## 23. 术语速查

| 术语 | 简单解释 |
| --- | --- |
| GameRoot | 游戏启动入口，负责创建和调度系统 |
| GameContext | 给系统用的公共工具包 |
| System | 纯 C# 玩法规则模块 |
| View | Unity 场景里的表现层，通常继承 MonoBehaviour |
| Config | ScriptableObject 配置数据 |
| Event | 系统之间传递的消息 |
| Tick | 纯 C# 系统的更新时间函数 |
| SaveModule | 需要交出存档数据的系统 |
| Data | 当前运行时数据或存档数据 |
| Manager | 通用基础服务，例如 UIManager、SaveManager |
| DialogueNode | 一次文案推进所呈现的文本、CG、立绘和特效数据 |
| DialogueEffect | 一次节点切换中由 View 完整执行的组合特效 |


