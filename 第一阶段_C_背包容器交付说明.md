# 第一阶段 C：背包与场景容器交付说明

## 已交付功能

- 玩家背包固定为 `3 x 3`；背包内按 `ItemConfig.BackpackFootprint` 判定占格，消防斧等 `1 x 2` 物品不能越界或重叠。
- 场景容器固定为 `2 x 2`，打开容器后与背包、副手栏、三格快捷栏同时显示。
- 支持点击两次或拖拽移动物品；落到同类物品时按最大堆叠数合并，落到不同物品时会在两端均合法的前提下交换。
- 快捷栏只允许配置了 `CanEquipToShortcut` 的道具；副手栏只允许配置了 `CanEquipToOffhand` 且具有副手类型的道具。
- 容器内容、背包、快捷栏与副手栏均纳入 `InventorySystem` 的存档数据；容器关闭后保留当前物品状态。
- 移动成功发布 `InventoryChangedEvent`，失败发布 `InventoryMoveFailedEvent`，快捷栏变更额外发布 `ShortcutChangedEvent`。

## 第一阶段容器初始内容

| 容器 | 初始道具 |
| --- | --- |
| `stage1_demo_container_01` | 制式刺刀 `1002`、消防斧 `1003`、瓶装气泡水 `1012` |
| `stage1_demo_container_02` | 手枪子弹 `1020`（6）、手提灯 `1006`、办公室钥匙 `1024` |

## 验收操作

1. 从 MainMenu 进入 SampleScene，靠近任一容器并按 `E`。
2. 将容器物品拖到背包空位，确认格子占用、数量和关闭后再次打开的状态一致。
3. 把两个不同道具互拖，确认交换；把同类可堆叠物品互拖，确认数量合并。
4. 将手提灯拖入副手栏；将可快捷装备的道具拖入快捷栏，按 `1`、`2`、`3` 选择。
5. 在存档点打开存档面板，存档后继续调整并读取，确认背包与容器状态恢复。
