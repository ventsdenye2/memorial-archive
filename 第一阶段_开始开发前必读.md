# 第一阶段开始开发前必读

本文件与仓库外的《第一阶段详细分工》3.5 节共同构成第一阶段接口合同。任何人口头约定与本文件冲突时，先在问题记录中提出，由主程序员 C 修改合同后再实施。

## 1. 当前基线

- 分支：`develop`。
- Unity：`2022.3.62f2c1`。
- 启动场景：`Assets/Scenes/MainMenu.unity`。
- 玩法场景：`Assets/Scenes/SampleScene.unity`。
- 配置总表：`Assets/GameConfigs/GameConfigDatabase.asset`。
- 一次性生成器：`Assets/Editor/Stage1ProjectBootstrap.cs`。基线已生成，正常开发不要重复运行。

拉取代码后先做一次以下检查：

1. Unity Console 没有编译错误或 Missing Script。
2. Build Settings 中 MainMenu 为 0，SampleScene 为 1。
3. 从 MainMenu 点击新游戏可以进入 SampleScene 黑屏剧情。
4. 直接打开 SampleScene 时，备用 GameRoot 能初始化且场景中只有一个有效 GameRoot。

## 2. 公共接口禁止各自修改

以下文件属于冻结接口，A/B/D 不直接修改，C 负责公共变更合并：

- `Assets/Scripts/Framework/Event/GameEvents.cs`
- `Assets/Scripts/Framework/Core/GameRoot.cs`
- `Assets/Scripts/Framework/Core/GameContext.cs`
- `Assets/Scripts/Framework/UI/UIManager.cs`
- `Assets/Scripts/Framework/UI/BasePanel.cs`
- `Assets/Scripts/Framework/UI/ScenePanelRegistry.cs`
- `Assets/Scripts/Framework/UI/PanelId.cs`
- `Assets/Scripts/Framework/Scene/SceneFlowManager.cs`
- `Assets/Scripts/Framework/Save/SaveManager.cs`
- `Assets/Scripts/Framework/Config/ConfigManager.cs`
- `Assets/Scripts/Framework/Config/GameConfigDatabase.cs`
- `Assets/Scripts/Gameplay/Stage1/Stage1Ids.cs`

需要新增事件时，在自己的交付说明中写明事件名、发布者、订阅者和参数，由 C 一次性加入 `GameEvents.cs`，避免四个人同时改一个文件。

## 3. 四人的实际编辑边界

### A

- 独占 MainMenu 场景和 UI/剧情外壳。
- `InventoryPanel.prefab` 只修改根对象及 `PanelChromeRoot`。
- `GameplayHUD.prefab` 只修改 `HudChromeRoot`。
- 不直接修改 SampleScene。

### B

- 独占 Character、Camera、Player.prefab、MainCamera.prefab、Stage1Player.asset。
- 不直接修改 MainMenu、SampleScene 和 UI Prefab。
- B 交付 Prefab 后，由 C 放入/更新 SampleScene。

### C

- 独占 SampleScene、GameConfigDatabase、Inventory、Interaction、Stage1、交互 Prefab 和最终整合。
- 背包内容只写入 `InventoryPanel.prefab/InventoryContentRoot`。
- HUD 快捷栏只写入 `GameplayHUD.prefab/ShortcutMount`。
- 不改 A 的 PanelChromeRoot/HudChromeRoot，不改 B 的移动核心代码。

### D

- 只改六个 ItemConfig 和两份测试文档。
- 不改场景、Prefab、GameConfigDatabase 和公共脚本。

## 4. 固定接口速查

| 项目 | 固定值 |
| --- | --- |
| storyId | 字符串 `"1"` |
| 玩家属性/状态 ID | `10001 / 10001` |
| 背包/容器/快捷栏/副手 | `3x3 / 2x2 / 3 / 1` |
| 速度 | 配置 `100/300`，移动层乘 `0.01` |
| 体力 | 最大 30，奔跑 -2/s，恢复 +1/s，闪避 -6 |
| 闪避 | 120 设计单位，0.18s，冷却 0.8s，无无敌帧 |
| 存档槽 | UI 1/2/3，对应代码 0/1/2，内存保存，可覆盖，不写磁盘 |
| 暂停 Panel | System、Settings、Save、Load、ConfirmDialog、BlackScreenStory |
| 非暂停 Panel | Inventory、Container、ShortcutBar、Diary、Map、HUD |

## 5. 每次交付必须附带

- 实际修改文件清单。
- 没有修改但依赖的公共接口清单。
- Unity Console 截图或明确的 0 Error 记录。
- 从哪个场景开始、如何复现、预期结果。
- 已知问题必须分为：配置、场景挂接、核心代码、视觉占位。

不要用“功能已完成”代替上述交付信息。

## 6. 本次基线提交边界

当前工作区还存在未纳入第一阶段基线的原始美术目录、`New Scene.unity` 和批量删除记录。提交 develop 时禁止使用 `git add -A`。本次基线只包含以下范围：

- `Assets/Editor/Stage1ProjectBootstrap.cs` 及其 `.meta`。
- `Assets/GameConfigs`、`Assets/Prefabs`。
- `Assets/Scenes/MainMenu.unity` 及其 `.meta`、`Assets/Scenes/SampleScene.unity`。
- `Assets/Art/Scenes/休息室.png` 及其 `.meta`（SampleScene 的当前背景依赖）。
- 本文件列出的冻结 Framework/Gameplay 脚本，以及新增的 `Assets/Scripts/Gameplay/Stage1`。
- `ProjectSettings/EditorBuildSettings.asset` 和本文件。

不要把 `Assets/Scenes/New Scene.unity`、`Assets/Actions`、其余原始美术素材、`原始策划需求` 或现有批量删除记录混入该提交；它们应由资源所有者另行确认。
