# 第一阶段验收报告

验收日期：2026-07-20
验收分支：`develop`

## 结论

- 第一阶段 29 项验收路线均已通过代码、场景或运行时验证，主流程没有阻塞型报错。
- A 的 UI 分支不能直接整分支合并：该分支与 `develop` 没有共同祖先，并包含会把现有背包实现覆盖成空类的 `InventoryPanel.cs`，以及职责错误的 `StopPanel.prefab`。
- 已按定位参考图补齐第一阶段 UI，主菜单使用正式封面并移除不需要的“继续游戏”入口。
- Unity EditMode/PlayMode 测试入口均返回 Passed，但工程当前没有登记实际测试用例；因此本报告主要依据运行时逐项验证，而不是把空测试套件当作验收依据。

## 29 项逐项结果

| # | 结果 | 验证记录 |
|---:|:---:|---|
| 1 | 通过 | Build Settings 中 `MainMenu.unity` 为索引 0，`SampleScene.unity` 为索引 1。 |
| 2 | 通过 | 运行时显示新游戏、读取存档、设置、退出四个按钮；使用正式封面且所有图片引用有效。 |
| 3 | 通过 | 点击新游戏后确认窗口可见、可交互，并暂停游戏时间。 |
| 4 | 通过 | 取消后窗口关闭，`Time.timeScale` 恢复为 1，主菜单可继续操作。 |
| 5 | 通过 | 再次新游戏并确认可以触发场景切换。 |
| 6 | 通过 | 活动场景切换为现有 `SampleScene`，没有创建第三张场景。 |
| 7 | 通过 | `SampleScene` 启动黑屏剧情；配置读取 `storyId=1`，文本资源存在且内容长度为 488。 |
| 8 | 通过 | 揭示过程中首次按键调用补全文字，不关闭剧情。 |
| 9 | 通过 | 再次按键结束剧情，黑屏 Panel 关闭、时间恢复、测试区和 HUD 可用。 |
| 10 | 通过 | 输入层和角色逻辑均压制 Y；运行时移动方向为 `(1,0)`；刚体冻结 Y，玩家、障碍和边界均有非 Trigger 碰撞体。 |
| 11 | 通过 | 配置和运行时均为步行 120、奔跑 340；运行 1 秒体力从 30 降到 28。 |
| 12 | 通过 | 停止奔跑后按 1/秒恢复，并在 30 封顶。 |
| 13 | 通过 | 闪避从 30 扣到 24；体力为 3 时再次请求闪避被拒绝且数值不变。 |
| 14 | 通过 | 右键对应的瞄准事件可把角色切为 Aiming，并更新瞄准方向。 |
| 15 | 通过 | 容器交互触发后同时打开 `ContainerPanel`、`InventoryPanel`、`ShortcutBarPanel`，并记录正确容器 ID。 |
| 16 | 通过 | 1×1 物品可从场景容器移入背包空格。 |
| 17 | 通过 | 消防斧进入背包后占用 `2×1`，逻辑不提供旋转为 `1×2` 的路径。 |
| 18 | 通过 | 填满 3×3 背包后，下一次加入返回失败并发布移动失败事件。 |
| 19 | 通过 | 可装备武器可移入 0/1/2 快捷格并由数字键 1/2/3 选中；HUD 显示三格有效快捷栏。 |
| 20 | 通过 | 手提灯进入快捷栏被拒绝，进入唯一副手栏成功；HUD 第四格独立显示副手。 |
| 21 | 通过 | 办公室钥匙进入快捷栏被拒绝。 |
| 22 | 通过 | 物品移回当前容器，关闭后重新打开同一容器，实例仍然存在。 |
| 23 | 通过 | `TAB` 可开关背包，关闭后不残留输入阻塞。 |
| 24 | 通过 | `I` 可开关正式日记页；弹窗、翻页、关闭和日记/纸条标签均按定位参考图排布。 |
| 25 | 通过 | `M` 可开关地图页占位。 |
| 26 | 通过 | `ESC` 打开系统页并将时间设为 0；`ContinueButton` 关闭系统页并恢复为 1。 |
| 27 | 通过 | 玩家位于 `stage1_save_point_01` 时触发交互，先打开 `SavePanel`。 |
| 28 | 通过 | `Slot0` 连续触发两次均完成保存；第二次 `OverwroteExisting=True`，无报错；共配置 3 个栏位。 |
| 29 | 通过 | 主菜单“读取存档”绑定占位方法并打开 Load Panel；关闭后时间与输入恢复，不会卡死。 |

## UI 合并边界

- 接收：`MainMenu.unity`、`MainMenuPanel`、`NewGameConfirmPanel`、`GameplayHUD`、`DiaryPanel` 及其必要封面按钮图片。
- 拒绝：A 分支的空 `InventoryPanel.cs`，避免覆盖 C 的完整背包实现。
- 拒绝：A 分支的 `StopPanel.prefab`，其实际挂接主菜单控制器，职责与系统页不符。
- `develop` 上已有的系统、背包、存档、容器和场景逻辑全部保留；背包与系统页仅重做表现层和按钮接线。
- 所有 UI `Selectable.navigation` 均设为 `None`。运行时先选中 `SelectButton`，再输入 `W/A/S/D/1/2/3`，选中对象仍为 `SelectButton`。

## 0719 Spine 动作包复验

- 用 `action (0719重命名版).zip` 中的新 JSON 覆盖并强制重新导入现有骨骼、Atlas 和贴图，保留原 GUID。
- SkeletonData 成功解析 10 个动画，并生成 10 个独立 `AnimationReferenceAsset` 供逐动作引用。
- `Block`、3 个单手动作、`death`、`dodge`、`hurt1`、`throw`、`throw_aim` 均可解析；源包中的 `back_jump` 时长为 0 秒，是美术源文件仍需补关键帧的问题。

## 工作区保护

验收和提交没有纳入原工作区中已有的以下未提交改动：

- `Assets/Scenes/SampleScene.unity`
- `ProjectSettings/TagManager.asset`
