# 音效接入说明

## 架构与配置

GameRoot 注册 AudioSystem，订阅已通过玩法校验的事件；AudioPlayback 负责播放、音量、变体和循环生命周期。配置位于 `Assets/Resources/Audio/GameAudioCatalog.asset`，运行时通过 Resources 加载，音频引用会随构建进入游戏。原始 WAV 保持原路径。

目录包含现有 58 个 WAV、40 个音效条目、17 个场景配置。编辑 Inspector 中的 clips、volume、bus、loop、cooldown 即可调整，无需修改玩法代码。脚步使用 sequential，从第一个 clip 开始按 1、2 交替；其他变体避免连续重复。单次声音默认限频 60ms；播放器最多 32 路，满时忽略新请求，不无限创建对象。缺失音效静默跳过，不以错误素材代替。

四路音量为 Sfx、UI、Ambience、Music，并有总音量。设置面板自动补充总音量滑条，PlayerPrefs 保存偏好；已有自定义滑条时优先使用 Inspector 绑定或面板内现有滑条。分路控制接口在 SettingsPanel 中，尚未添加四个额外 UI 控件。

## 已接入的触发点

| 需求 | 接入方式 |
| --- | --- |
| P-01～06 脚步 | 由 Spine 动画 footstep 事件触发，普通跑步使用 sfx_player_run_wood_1/2、走路使用 sfx_player_footstep_wood_1/2 顺序交替；实际位移只用于屏蔽撞墙和停步，不再计步。不对单步 WAV 设置 loop |
| P-08、09、10、13 | 已批准闪避、实际收到伤害、成功格挡、死亡事件；致死一击只播放死亡声 |
| P-14、23 | 装备匕首、已批准的匕首近战攻击，随机选择攻击变体 |
| M-01～07 | 幽灵追逐循环；守卫挥击、诊疗师实际攻击帧、受击硬直、死亡。以玩家水平距离做 0～12 世界单位衰减 |
| S-01 | 有效房间切换完成后播放开门声；读档不播放，不对取消或无效场景请求播放 |
| S-04 | 实际打开容器面板时播放，兼容当前容器复用背包面板的实现 |
| S-05 | NoteUnlockedEvent 播放纸条拾取声 |
| S-07 | 存档点交互播放电话铃一次，不循环，也不把任意打开存档菜单当作电话交互 |
| S-09 | 手提灯亮灭事件，单次随机变体 |
| U-01、02 | 主菜单 Button 悬浮和成功点击，自动绑定；禁用按钮不响 |
| U-03、05～08 | 设置/系统、背包、日记、地图打开及非空物品选中事件 |
| U-09 | 对话推进输入，支持鼠标和空格 |
| A-01、02、04、07～09 | 前厅、办公室、档案室、接待室、厕所、楼层的环境音循环 |

循环仅启用于六条 30 秒环境音和十秒幽灵移动音。动作音在暂停时 Pause/UnPause，UI 和环境音继续播放。环境音进入场景后淡入；场景切换立即清理原场景声音。幽灵停止追逐时淡出，禁用/销毁时清理循环，死亡单次声音允许播完。

当前全部场景均为室内木地板：17 个 profile.surface 和未知场景默认值统一为 wood。大理石/石质音频保留在目录中，当前场景不使用。Floor 场景仍使用楼梯环境音。

脚步标记根据 Spine 腿部姿态采样放在 JSON 动画时间线上，普通跑步/提灯跑步为 0.1、0.4、0.7、1.0 秒（片段 1.2 秒）；普通/提灯走路为 0、0.6、1.1、1.6 秒（片段 2.1 秒）；重伤跑步为 0.1667、0.8 秒；重伤走路为 0.0333、0.8 秒。边走边装备有独立六个标记。事件的 int=1 选择跑步音，int=0 选择行走音。

CharacterAnimationView 只转发当前 TrackEntry 的 footstep 事件。Spine 自己处理变速、循环和暂停；装备动画切换时同步 AnimationLast，避免补播已经经过的落脚。重新导出 Spine JSON 时需保留名为 footstep 的事件轨，或由美术在实际接触地面的帧重新标记；不应恢复按移动距离或固定秒数计步。

## 已有素材但入口待确定

- S-06 日记残页拾取：当前没有独立的日记残页拾取流程，不能把打开日记 UI 当作拾取。`AudioCueEmitter.DiaryPickedUp()` 已准备好。
- U-04 任务展开/收起：当前 HUD 未找到任务折叠逻辑。`AudioCueEmitter.QuestToggled()` 已准备好。

上述两个入口已向需求方询问。将 AudioCueEmitter 挂到相关对象后，可从 UnityEvent/动画事件调用；也可在完成操作的位置调用 `AudioSystem.Play("sfx_scene_diary_pickup")` 或 `AudioSystem.Play("sfx_ui_quest_toggle")`。

## 缺失素材与后续接入

当前素材目录中未找到以下内容，尚不能完成实际发声：

- P-07 重伤移动；P-11/12 独立盾牌/钢夹板受击；P-15/16/18/20～22 装备；P-24/25 刺刀/斧攻击；P-27～29 手枪；P-33～35 投掷物。
- S-02 铁门阻挡、S-08 钥匙、S-10 绳索；I-01～08 消耗品。
- A-03 露台、A-05 诊疗室、A-06 禁闭室；H-01/02/03/07/08 随机恐怖音；BGM-01～04 全部配乐。

武器装备/近战和手枪出枪帧已预留 `sfx_player_equip_{dagger|bayonet|axe|pistol}`、`sfx_player_attack_{dagger|bayonet|axe}`、`sfx_player_shoot_pistol`；钥匙事件预留 `sfx_scene_key_unlock`。场景音乐字段预留 bgm_menu、bgm_explore、bgm_safe。未来添加这些目录条目即可发声。

其他缺失项仍需拿到素材后对齐实际帧/事件，尤其：燃烧瓶应按撞击与火区存活分层，手雷按拉环与爆炸分开，换弹音按实际换弹过程，战斗 BGM 按交战状态切换，随机恐怖音需场景白名单与冷却调度。这些不能仅勾选 loop 便视为完成，目前没有伪造对应素材或新增随机事件。

需求表明确“目前不做”的佩剑、霰弹枪、记忆碎片、座钟及异变/同化项未扩展玩法。

## 验证

- 125 个运行时 C# 文件使用项目现有 Unity 引用编译通过。
- 58 个 WAV 引用与目录 GUID 对应，检查音频头和时长；单步 0.5 秒、跑步单步 0.25 秒，适合程序触发。
- git diff 空白检查通过。
- 使用项目自带 Spine C# 运行库验证七套脚步时间线，并对六套循环步态测试 0.5、1、2 倍速、零时间暂停和动画替换；事件数量与各动画标记一致，暂停和替换后没有旧轨道重复事件。
- 尚未在 Unity Play Mode 中试听或执行场景交互验收；音量平衡、循环接缝和步频仍需实际听感确认。

验收顺序：主菜单悬浮/点击 → 设置滑条及重新打开 → 前厅走跑/撞墙/闪避 → 背包/柜子/纸条/电话 → 关开灯 → 木地板房间切换 → 两类怪物追逐/攻击/受伤/死亡 → 暂停恢复 → 读档和回主菜单。观察 GameRoot 下 Game Audio 的源数量不超过 32，并确认已离开场景无遗留循环。
